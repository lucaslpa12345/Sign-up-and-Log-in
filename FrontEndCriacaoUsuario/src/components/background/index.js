import LinearGradiant from 'react-native-linear-gradient';
import styled from 'styled-components/native';

export default styled(LinearGradiant).attrs({
  colors: ['#F7971E', '#FFD200'],
})`
  flex: 1;
`;
